# Clone

cd into directory.
cd into frontend dir,

## Available Scripts

In the fronend directory, you can run:

### `npm install`

### `npm start`

In the backend directory, you can run:

### npm install

### `npm start`
